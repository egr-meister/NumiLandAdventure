# Copy this file to "keystore.properties" for LOCAL release builds only.
# NEVER commit the real keystore.properties or your .keystore file.
# In CI, these values come from GitHub Secrets instead (see README).

storeFile=release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD
